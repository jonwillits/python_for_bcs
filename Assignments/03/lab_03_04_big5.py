'''
    Re-write the big-5 program from last week so that it it broken up into a functionally organized program that
    has the following functions:
    - a main function

    - a "load_survey()" function that loads the questions and instructions from a file (or multiple files)
    - a "present survey()" function that asks each survey question and records the responses
    - a "calculate_scores()" function that calculates the final OCEAN scores
    - a "print_results()" function that prints the results to the screen
    - a "save_results()" function that saves the results to a file
'''